MySQL is a relaional database management system (RDBMS) that runs on a server.
We use Strucured Query Language (SQL) to coomunicate with the database.
MySQL use tables to store data.
It can contain muiltiple tables.
We can use foriegn keys to link tables together.

video #23