If we want to access external files we need to use include or require.

When we use include, if the file is not found, we will get a warning but the script will continue to run.

But if we use require, if the file is not found, we will get a fatal error and the script will stop.

video #15