2. Associative Arrays

    - These are key value pairs
    - We initialize them like this:
        $arrayName = ['key' => 'value', 'key' => 'value', key => 'value'];
    - We access the values like this:
        $arrayName['key'];

video #7